function websiteHumanModeHint(mode){if(mode==='contact_only')return 'The AI gives the configured contact details instead of transferring the live chat.';if(mode==='ai_only')return 'The AI continues helping without offering a transfer or contact route.';return 'The conversation can transfer to a human when the Human Handoff action is configured for this employee.'}
function updateWebsiteHumanModeUI(){const mode=document.getElementById('web-human-mode')?.value||'direct_handoff';const hint=document.getElementById('web-human-mode-hint');if(hint)hint.textContent=websiteHumanModeHint(mode);const contacts=document.getElementById('web-human-contacts');if(contacts)contacts.style.display=mode==='contact_only'?'block':'none'}
async function openWebsiteChannel(companyId,agentId){try{const r=await api(`/admin/website-channel/agents/${agentId}`),d=r.config||{},mode=d.human_assistance_mode||'direct_handoff';openModal("Website Chat",`<p>Website Chat is only the communication surface. The same AI employee identity, Knowledge and Actions are used here automatically.</p><div class="form-group"><label>Website Domain</label><input id="web-domain" placeholder="example.com" value="${f(d.allowed_domain)}"></div><div class="form-group"><label>Chat Name</label><input id="web-name" value="${f(d.widget_name)}"></div><div class="form-group"><label>Welcome Message</label><input id="web-welcome" value="${f(d.welcome_message)}" placeholder="مرحباً، كيف يمكنني مساعدتك؟"></div><div class="form-group"><label>Position</label><select id="web-position"><option value="right">Right</option><option value="left">Left</option></select></div><div class="form-group"><label>Launcher Text</label><input id="web-label" value="${f(d.launcher_label||'Chat')}"></div><div class="form-group"><label>Brand Color</label><input id="web-color" type="color" value="${f(d.accent_color||'#111827')}"></div><div class="form-group"><label>Human Assistance</label><select id="web-human-mode" onchange="updateWebsiteHumanModeUI()"><option value="direct_handoff">Direct Handoff</option><option value="contact_only">Contact Only</option><option value="ai_only">AI Only</option></select><div id="web-human-mode-hint" class="meta" style="margin-top:6px"></div></div><div id="web-human-contacts"><div class="form-group"><label>Contact Phone</label><input id="web-contact-phone" value="${f(d.contact_phone)}" placeholder="+968..."></div><div class="form-group"><label>Contact WhatsApp</label><input id="web-contact-whatsapp" value="${f(d.contact_whatsapp)}" placeholder="+968..."></div><div class="form-group"><label>Contact Email</label><input id="web-contact-email" type="email" value="${f(d.contact_email)}" placeholder="support@example.com"></div><div class="form-group"><label>Contact / Booking URL</label><input id="web-contact-url" value="${f(d.contact_url)}" placeholder="https://..."></div></div><div class="form-group"><label>Website-only Instructions (optional)</label><textarea id="web-instructions" placeholder="Only rules specific to this website channel. Do not repeat business facts or action setup here.">${f(d.custom_instructions)}</textarea></div><button class="modal-submit" onclick="saveWebsiteChannel(${companyId},${agentId})">Save Website Chat</button>${r.configured?`<div style="margin-top:16px"><div class="meta">${r.enabled?'ACTIVE':r.ready?'READY TO ACTIVATE':'SETUP REQUIRED'}</div>${(r.blockers||[]).map(x=>`<div class="meta">${f(x)}</div>`).join('')}<div class="form-group"><label>Embed Code</label><textarea readonly>${f(r.embed_code)}</textarea></div><div class="agent-actions">${r.enabled?`<button class="table-button" onclick="deactivateWebsiteChannel(${companyId},${agentId},${r.channel_id})">Deactivate</button>`:r.ready?`<button class="table-button" onclick="activateWebsiteChannel(${companyId},${agentId},${r.channel_id})">Activate</button>`:''}</div></div>`:''}`);setTimeout(()=>{if(document.getElementById('web-position'))document.getElementById('web-position').value=d.position||'right';if(document.getElementById('web-human-mode'))document.getElementById('web-human-mode').value=mode;updateWebsiteHumanModeUI()},0)}catch(e){alert(e.message)}}
async function saveWebsiteChannel(c,a){try{const g=id=>document.getElementById(id);await api(`/admin/website-channel/agents/${a}`,{method:'PUT',body:JSON.stringify({allowed_domain:g('web-domain').value.trim(),widget_name:g('web-name').value.trim()||null,welcome_message:g('web-welcome').value.trim()||null,position:g('web-position').value,launcher_label:g('web-label').value.trim()||'Chat',accent_color:g('web-color').value,custom_instructions:g('web-instructions').value.trim()||null,human_assistance_mode:g('web-human-mode').value,contact_phone:g('web-contact-phone').value.trim()||null,contact_whatsapp:g('web-contact-whatsapp').value.trim()||null,contact_email:g('web-contact-email').value.trim()||null,contact_url:g('web-contact-url').value.trim()||null})});await openWebsiteChannel(c,a)}catch(e){alert(e.message)}}
async function activateWebsiteChannel(c,a,id){try{await api(`/admin/website-channel/${id}/activate`,{method:'POST'});await openWebsiteChannel(c,a)}catch(e){alert(e.message)}}
async function deactivateWebsiteChannel(c,a,id){try{await api(`/admin/website-channel/${id}/deactivate`,{method:'POST'});await openWebsiteChannel(c,a)}catch(e){alert(e.message)}}
