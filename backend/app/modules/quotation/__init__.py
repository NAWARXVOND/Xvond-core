from backend.app.core.module import BaseModule


class QuotationModule(BaseModule):
    name = "quotation"
    version = "1.0.0"
    description = "Quotation requests, pricing workflows and quote lifecycle capability"
